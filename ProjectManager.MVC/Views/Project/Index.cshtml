@model IEnumerable<ProjectManager.Application.Project.ProjectDto>

@{
    ViewData["Title"] = "Projects";
}

<div class="row d-flex justify-content-center flex-column align-items-center">
    <h1 class="text-center">Projects</h1>
    <p class="text-center">
        <a asp-action="Create" class="btn btn-outline-primary">Create New</a>
    </p>
</div>

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th>
                <span>Finish Date</span>
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FinishDate)
                </td>
                <td style="display: flex; justify-content: flex-end; align-items: center;">
                    <a class="btn btn-outline-primary" asp-controller="Project" asp-action="Details" asp-route-encodedName="@item.EncodedName">Details</a>
                    <a class="btn btn-outline-secondary" asp-controller="Project" asp-action="Edit" asp-route-encodedName="@item.EncodedName">Edit</a>
                    <a class="btn btn-danger" asp-controller="Project" asp-action="Remove" asp-route-encodedName="@item.EncodedName">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
